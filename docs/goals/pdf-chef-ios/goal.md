# PDF Chef iOS Production-Ready Application

## Objective

Deliver a polished, offline-capable PDF Chef iOS application from the existing React/Vite interface and Capacitor iOS project. Claude Code Opus 5 High exclusively owns all visible design and frontend UI work. Codex exclusively owns non-visual engineering, native integration, persistence, testing, diagnostics, and final functional verification.

## Original Request

Create and open a local live GoalBuddy visual board, then continue autonomously through design, implementation, integration, testing, and final audits without stopping after planning or the first successful build.

## Intake Summary

- Input shape: `existing_plan`
- Audience: iPhone and iPad users handling private PDF documents
- Authority: `approved`
- Proof type: `test`, `demo`, `artifact`, and `review`
- Completion proof: authenticated Claude Code Opus 5 High UI acceptance, Codex engineering acceptance, passing automated checks, installed and launched simulator build, persisted settings/history, working local import/storage/save/share, verified offline assets, responsive/accessibility evidence, and unchanged Kotlin Android reference project
- Likely misfire: shipping a visually generic or Codex-designed frontend, stopping at a web build, using temporary browser URLs for history, relying on remote assets, or claiming unperformed device/App Store verification
- Blind spots considered: exact Claude model availability and authentication, current non-Git workspace state, simulator/runtime availability, offline OCR/PDF worker packaging, iPad support, share-sheet automation limits, and pre-existing PDF algorithm defects
- Existing plan facts: strict two-owner workflow; React/Vite and Capacitor iOS architecture; three-tab Home/Recent/Settings product; typed platform adapters; local persistence; read-only Android reference; named build, simulator, offline, functional, screenshot, accessibility, and final-audit gates

## Goal Kind

`existing_plan`

## Current Tranche

Continuously validate the supplied plan, map the current project, delegate every visible frontend decision and edit to authenticated Claude Code Opus 5 High, implement non-visual engineering in bounded Codex slices, integrate and verify each slice, then complete simulator, offline, functional, visual, responsive, accessibility, and ownership audits until the full owner outcome is proven.

## Non-Negotiable Constraints

- Claude Code Opus 5 High exclusively owns information architecture, React UI components, CSS, visual tokens, copy, accessibility presentation, touch behavior, motion, haptics design, artwork, screenshots, and final UI acceptance.
- Codex must not design, restyle, polish, or independently change the visible frontend.
- Claude must read Apple Design, Impeccable, and Brand skill instructions completely before design work and must run Impeccable's required project-context loader.
- Claude must follow existing `PRODUCT.md` and `DESIGN.md`, create `docs/brand-guidelines.md`, and synchronize `assets/design-tokens.json` with `assets/design-tokens.css`.
- Shared UI, settings/history models, business logic, and PDF workflows remain in TypeScript; the iOS application remains Capacitor-based and is not rewritten in SwiftUI.
- Native iOS work stays under `ios/`; web and iOS adapters must remain suitable for a future Capacitor Android implementation.
- Preserve fully offline operation and bundle application assets, OCR, PDF.js, workers, fonts, and WebAssembly without a remote application server URL.
- Never upload files, filenames, history, or other private document metadata.
- `/Users/dhananjaybhosale/AndroidStudioProjects/PDFTools` is read-only reference material and must remain unchanged.
- The current project is non-Git. Do not initialize Git, reset, clean, overwrite unrelated files, or imply commit-level evidence.
- Use file manifests and hashes to bind before/after evidence to this checkout.
- Do not claim physical-device, App Store, or production verification unless actually performed.
- Major pre-existing PDF algorithm defects may be `DEFERRED` only when they require a separate processing rewrite; they must not crash the application shell or lose documents.

## Stop Rule

Stop only when a final audit proves the full original owner outcome is complete.

Do not stop after planning, discovery, a web build, Judge selection, or the first simulator launch while safe required work remains.

If exact Claude Code Opus 5 High is unavailable or unauthenticated, block only Claude-owned UI tasks, continue safe non-UI engineering, and request direction only after all safe local work is exhausted.

## Canonical Board

Machine truth lives at:

`docs/goals/pdf-chef-ios/state.yaml`

If this charter and `state.yaml` disagree, `state.yaml` wins for task status, active task, receipts, verification freshness, and completion truth.

## Run Command

```text
/goal Follow docs/goals/pdf-chef-ios/goal.md.
```

## PM Loop

1. Read this charter and `state.yaml`.
2. Re-run the GoalBuddy update checker when appropriate without upgrading tooling unless authorized.
3. Work only on the active task and enforce its owner and write scope.
4. Record authenticated Claude model/session evidence for every Claude-owned task.
5. Record compact receipts and current verification in `state.yaml`.
6. Advance immediately to the next safe required task.
7. Finish only after a skeptical final audit maps every completion requirement to current evidence and records `full_outcome_complete: true`.

