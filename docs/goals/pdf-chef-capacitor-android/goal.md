# PDF Chef Capacitor Android Emergency Migration

## Objective

Migrate the live PDF Chef Android application to the shared React/Vite/Capacitor product without losing production identity, user files, history, settings, offline behavior, native PDF capabilities, or acceptable steady-state performance. Continue through discovery, architecture, implementation, integration, genuine in-place upgrade testing, performance evidence, and final audits. Do not publish or change Play state without explicit approval.

## Original Request

Create and open a local live GoalBuddy visual board, then autonomously complete the PDF Chef Capacitor Android compatibility migration using maximum useful parallelism and evidence-backed acceptance.

## Intake Summary

- Input shape: `existing_plan`
- Audience: more than 10,000 existing PDF Chef Android users plus future web and iOS users of the shared product
- Authority: `approved` for local read-only discovery, planning, bounded implementation, local builds, and non-destructive testing; `needs_approval` for credentials, signing secrets, Play Console writes, uploads, track changes, or rollout
- Proof type: `test`, `artifact`, `metric`, `review`, and `demo`
- Completion proof: every item in the source objective's COMPLETION section is proven by current artifacts, genuine update tests, performance comparisons, Claude UI acceptance, Codex engineering acceptance, and separate public signing-certificate verification
- Likely misfire: producing a clean new Android shell that passes fresh-install tests but changes application identity, bypasses the shared React source, loses legacy history/settings/files, lacks native tool parity, performs poorly, or cannot upgrade the Play-installed application
- Blind spots considered: Play version drift, upload-vs-Play signing identity, corrupt legacy indexes, directories and missing files, process death, nearly full storage, WebView memory behavior, Kotlin fallback/rollback, UI ownership, and absence of physical-device or Play evidence
- Existing plan facts: the full objective at `/Users/dhananjaybhosale/.codex/attachments/600fff37-7686-4f93-9941-f6a8bcbeb320/goal-objective.md` is authoritative and must be preserved requirement-by-requirement

## Goal Kind

`existing_plan`

## Current Tranche

Validate the supplied migration plan against both current workspaces, choose the lowest-risk host architecture, establish exact file ownership and verification contracts, then execute successive safe non-UI engineering slices. Continue through integration and acceptance; do not stop at planning or the first successful build.

## Non-Negotiable Constraints

- The primary writable workspace is `/Users/dhananjaybhosale/Documents/PDFTools-main`.
- `/Users/dhananjaybhosale/AndroidStudioProjects/PDFTools` is a read-only compatibility, migration, performance, signing, manifest, and rollback reference unless the user explicitly authorizes changes.
- Both projects are shared non-Git workspaces. Do not initialize Git, create worktrees, reset, clean, stash, overwrite unrelated work, or cross repository boundaries with writes.
- The production application ID remains exactly `com.dhananjaytech.pdfchef`; do not confuse it with Kotlin namespace `com.dhananjaytech.zenpdf_allpdftoolsinoneplace`.
- Never print, store, copy, request in chat, or commit passwords, keystores, aliases, or private keys.
- Do not upload, publish, modify Play tracks, force an update, or begin/increase a rollout without explicit approval.
- The same bundled React/Vite source must power web, iOS, and Android; Android must not load a remote site or require a Node server.
- Claude Code Opus 5 High exclusively owns all user-visible design decisions and React frontend implementation. Codex may only connect Claude-owned components to typed engineering interfaces. If that exact model is unavailable, UI work is blocked while safe non-UI engineering continues.
- Preserve legacy `files/processed_index.json`, `files/processed/`, and `app_settings` DataStore formats for the first compatibility release unless evidence proves an idempotent, atomic, restartable, versioned, path-contained, resumable, and non-destructive migration is unavoidable.
- Do not delete or substantially rewrite the Kotlin project. Preserve it as compatibility source, native fallback, and rollback reference.
- No document, filename, or contents telemetry; backup-disabled privacy policy remains unchanged without explicit approval.
- A locally upload-key-signed APK is not final update evidence. Final acceptance requires a Play internal-track update over an actual Play-installed predecessor.
- Any history, settings, signing, data-safety, startup-ANR, process-death, or steady-state performance failure forces `PRODUCTION_RELEASE_READY: NO`.

## Stop Rule

Stop only when a final Judge/PM audit proves the full original outcome is complete.

Do not stop after planning, discovery, architecture selection, a first Worker slice, or the first successful build. When credentials, Play access, physical hardware, destructive operations, or owner approval block one acceptance item, record that exact item as blocked or not checked and continue every safe local tranche.

## Canonical Sources

- Full owner objective: `/Users/dhananjaybhosale/.codex/attachments/600fff37-7686-4f93-9941-f6a8bcbeb320/goal-objective.md`
- Board truth: `docs/goals/pdf-chef-capacitor-android/state.yaml`
- Primary workspace: `/Users/dhananjaybhosale/Documents/PDFTools-main`
- Read-only Kotlin authority: `/Users/dhananjaybhosale/AndroidStudioProjects/PDFTools`

If this charter and `state.yaml` disagree, `state.yaml` wins for task status, active task, receipts, verification freshness, and completion truth. The full owner objective still wins for required scope and completion criteria.

## Run Command

```text
/goal Follow docs/goals/pdf-chef-capacitor-android/goal.md.
```

## PM Loop

1. Read this charter, the full owner objective, and `state.yaml`.
2. Keep exactly one active board task while using parallel read-only agents inside Scout/Judge tasks where useful.
3. Give every write-capable agent explicit, non-overlapping file ownership and verification commands.
4. Record compact receipts in `state.yaml`; use `notes/` only for long evidence.
5. Re-audit the original objective after each verified slice and immediately activate the next safe task.
6. Never convert missing production authority into permission. Continue local work and keep the blocked acceptance gate explicit.
7. Finish only with a final audit receipt containing `full_outcome_complete: true` and the exact required verdict matrix.
