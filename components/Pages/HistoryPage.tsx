/**
 * "History" became the Recent tab. The old name is kept as an alias so any
 * existing import or bookmark resolves to the one implementation instead of a
 * second, drifting copy of the same screen.
 */
export { RecentPage as HistoryPage } from './RecentPage';
